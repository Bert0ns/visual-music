# Run with `dart run ffigen --config ffigen.yaml`.
name: ProjectmFfiBindings
description: |
  Bindings for `src/projectm_ffi.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/projectm_ffi_bindings_generated.dart'
headers:
  entry-points:
    - 'src/projectm_ffi.h'
  include-directives:
    - 'src/projectm_ffi.h'
comments:
  style: any
  length: full
